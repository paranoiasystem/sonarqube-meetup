{
    "type": "module",
    "scripts": {
        "start": "node server.js",
        "test": "tap test/*.js",
        "test:coverage": "tap test/*.js --coverage-report=lcov"
    },
    "dependencies": {
        "fastify": "^4.23.2"
    },
    "devDependencies": {
        "tap": "^18.4.2"
    }
}
